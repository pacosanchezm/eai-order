import React from "react";
import ReactDOM from "react-dom";

import App from "../eai-order/cs1i";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <App 
      id={3872143}
    />
  </React.StrictMode>,
  rootElement
);
